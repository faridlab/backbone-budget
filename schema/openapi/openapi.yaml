openapi: '3.0.3'
info:
  title: Backbone API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    This API provides 11 standard CRUD endpoints for each model.

servers:
  - url: /
    description: Development server

tags:
  - name: Budgets
    description: Budget management endpoints
  - name: BudgetLines
    description: BudgetLine management endpoints

paths:
  /api/v1/budget/budgets:
    get:
      tags:
        - Budgets
      summary: List Budgets
      description: Get a paginated list of Budgets with optional filtering and sorting
      operationId: listBudgets
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - Budgets
      summary: Create Budget
      description: Create a new Budget
      operationId: createBudget
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBudgetRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/budget/budgets/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - Budgets
      summary: Get Budget by ID
      operationId: getBudget
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - Budgets
      summary: Update Budget (full)
      operationId: updateBudget
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBudgetRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - Budgets
      summary: Update Budget (partial)
      operationId: patchBudget
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchBudgetRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - Budgets
      summary: Delete Budget (soft)
      operationId: deleteBudget
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/budget/budgets/bulk:
    post:
      tags:
        - Budgets
      summary: Bulk create Budgets
      operationId: bulkCreateBudgets
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateBudgetRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Budget'
    put:
      tags:
        - Budgets
      summary: Bulk update Budgets (full, atomic)
      operationId: bulkUpdateBudgets
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateBudgetRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - Budgets
      summary: Bulk partial-update Budgets (atomic)
      operationId: bulkPatchBudgets
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budgets/upsert:
    post:
      tags:
        - Budgets
      summary: Upsert Budget
      operationId: upsertBudget
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBudgetRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'

  /api/v1/budget/budgets/trash:
    get:
      tags:
        - Budgets
      summary: List deleted Budgets
      operationId: listDeletedBudgets
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetList'

  /api/v1/budget/budgets/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - Budgets
      summary: Restore deleted Budget
      operationId: restoreBudget
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/budget/budgets/empty:
    delete:
      tags:
        - Budgets
      summary: Empty trash for Budgets
      operationId: emptyTrashBudgets
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/budget/budgets/delete/bulk:
    post:
      tags:
        - Budgets
      summary: Bulk soft-delete Budgets by ids (atomic)
      operationId: bulkDeleteBudgets
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budgets/restore/bulk:
    post:
      tags:
        - Budgets
      summary: Bulk restore Budgets by ids (atomic)
      operationId: bulkRestoreBudgets
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budgets/restore/all:
    post:
      tags:
        - Budgets
      summary: Restore all soft-deleted Budgets
      operationId: restoreAllBudgets
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/budget/budgets/trash/bulk:
    delete:
      tags:
        - Budgets
      summary: Bulk permanently-delete Budgets from trash by ids (atomic)
      operationId: bulkPermanentDeleteBudgets
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budget_lines:
    get:
      tags:
        - BudgetLines
      summary: List BudgetLines
      description: Get a paginated list of BudgetLines with optional filtering and sorting
      operationId: listBudgetLines
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - BudgetLines
      summary: Create BudgetLine
      description: Create a new BudgetLine
      operationId: createBudgetLine
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBudgetLineRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/budget/budget_lines/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - BudgetLines
      summary: Get BudgetLine by ID
      operationId: getBudgetLine
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - BudgetLines
      summary: Update BudgetLine (full)
      operationId: updateBudgetLine
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBudgetLineRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - BudgetLines
      summary: Update BudgetLine (partial)
      operationId: patchBudgetLine
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchBudgetLineRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - BudgetLines
      summary: Delete BudgetLine (soft)
      operationId: deleteBudgetLine
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/budget/budget_lines/bulk:
    post:
      tags:
        - BudgetLines
      summary: Bulk create BudgetLines
      operationId: bulkCreateBudgetLines
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateBudgetLineRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BudgetLine'
    put:
      tags:
        - BudgetLines
      summary: Bulk update BudgetLines (full, atomic)
      operationId: bulkUpdateBudgetLines
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateBudgetLineRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - BudgetLines
      summary: Bulk partial-update BudgetLines (atomic)
      operationId: bulkPatchBudgetLines
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budget_lines/upsert:
    post:
      tags:
        - BudgetLines
      summary: Upsert BudgetLine
      operationId: upsertBudgetLine
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBudgetLineRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'

  /api/v1/budget/budget_lines/trash:
    get:
      tags:
        - BudgetLines
      summary: List deleted BudgetLines
      operationId: listDeletedBudgetLines
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineList'

  /api/v1/budget/budget_lines/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - BudgetLines
      summary: Restore deleted BudgetLine
      operationId: restoreBudgetLine
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/budget/budget_lines/empty:
    delete:
      tags:
        - BudgetLines
      summary: Empty trash for BudgetLines
      operationId: emptyTrashBudgetLines
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/budget/budget_lines/delete/bulk:
    post:
      tags:
        - BudgetLines
      summary: Bulk soft-delete BudgetLines by ids (atomic)
      operationId: bulkDeleteBudgetLines
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budget_lines/restore/bulk:
    post:
      tags:
        - BudgetLines
      summary: Bulk restore BudgetLines by ids (atomic)
      operationId: bulkRestoreBudgetLines
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/budget/budget_lines/restore/all:
    post:
      tags:
        - BudgetLines
      summary: Restore all soft-deleted BudgetLines
      operationId: restoreAllBudgetLines
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/budget/budget_lines/trash/bulk:
    delete:
      tags:
        - BudgetLines
      summary: Bulk permanently-delete BudgetLines from trash by ids (atomic)
      operationId: bulkPermanentDeleteBudgetLines
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

components:
  schemas:
    Budget:
      type: object
      required:
        - id
        - code
        - name
        - fiscal_year
        - date_from
        - date_to
        - status
        - enforcement
        - metadata
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        description:
          nullable: true
          type: string
          maxLength: 500
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        status:
          type: string
        enforcement:
          type: string
        metadata:
          type: object

    BudgetBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Budget'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateBudgetRequest:
      type: object
      required:
        - code
        - name
        - fiscal_year
        - date_from
        - date_to
        - status
        - enforcement
        - metadata
      properties:
        code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        description:
          nullable: true
          type: string
          maxLength: 500
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        status:
          type: string
        enforcement:
          type: string
        metadata:
          type: object

    UpdateBudgetRequest:
      type: object
      required:
        - code
        - name
        - fiscal_year
        - date_from
        - date_to
        - status
        - enforcement
        - metadata
      properties:
        code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        description:
          nullable: true
          type: string
          maxLength: 500
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        status:
          type: string
        enforcement:
          type: string
        metadata:
          type: object

    PatchBudgetRequest:
      type: object
      properties:
        code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        description:
          nullable: true
          type: string
          maxLength: 500
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        status:
          type: string
        enforcement:
          type: string
        metadata:
          type: object

    BudgetList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Budget'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    BudgetLine:
      type: object
      required:
        - id
        - budget_id
        - account_id
        - fiscal_period_id
        - fiscal_year
        - planned_amount
        - metadata
      properties:
        id:
          type: string
          format: uuid
        budget_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        cost_center_id:
          nullable: true
          type: string
          format: uuid
        fiscal_period_id:
          type: string
          format: uuid
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        fiscal_month:
          nullable: true
          type: integer
          format: int32
          minimum: 1
          maximum: 12
        planned_amount:
          type: string
        notes:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    BudgetLineBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BudgetLine'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateBudgetLineRequest:
      type: object
      required:
        - budget_id
        - account_id
        - fiscal_period_id
        - fiscal_year
        - planned_amount
        - metadata
      properties:
        budget_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        cost_center_id:
          nullable: true
          type: string
          format: uuid
        fiscal_period_id:
          type: string
          format: uuid
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        fiscal_month:
          nullable: true
          type: integer
          format: int32
          minimum: 1
          maximum: 12
        planned_amount:
          type: string
        notes:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    UpdateBudgetLineRequest:
      type: object
      required:
        - budget_id
        - account_id
        - fiscal_period_id
        - fiscal_year
        - planned_amount
        - metadata
      properties:
        budget_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        cost_center_id:
          nullable: true
          type: string
          format: uuid
        fiscal_period_id:
          type: string
          format: uuid
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        fiscal_month:
          nullable: true
          type: integer
          format: int32
          minimum: 1
          maximum: 12
        planned_amount:
          type: string
        notes:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    PatchBudgetLineRequest:
      type: object
      properties:
        budget_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        cost_center_id:
          nullable: true
          type: string
          format: uuid
        fiscal_period_id:
          type: string
          format: uuid
        fiscal_year:
          type: integer
          format: int32
          minimum: 2020
          maximum: 2100
        fiscal_month:
          nullable: true
          type: integer
          format: int32
          minimum: 1
          maximum: 12
        planned_amount:
          type: string
        notes:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    BudgetLineList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BudgetLine'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  requestBodies:
    BatchIds:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [ids]
            properties:
              ids:
                type: array
                items: { type: string }

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
