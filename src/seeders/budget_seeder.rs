//! Budget seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Budget data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BUDGET SEEDER
// ============================================================================

/// Seeder for budget
pub struct SeedBudgetSeeder;

impl SeedBudgetSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBudgetSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBudgetSeeder {
    fn name(&self) -> &'static str {
        "SeedBudgetSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if budget.budgets table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM budget.budgets")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/budget_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM budget.budgets")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
